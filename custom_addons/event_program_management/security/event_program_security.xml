<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- 
            Event/Program Access Rules:
            - Ministry of Youth Sports and Arts staff: See all events
            - Other company staff: See only events from their own company
        -->
        
        <!-- Allow system users (admin, etc) to see all events -->
        <record id="rule_event_program_all_for_admin" model="ir.rule">
            <field name="name">Event/Program: Admin sees all</field>
            <field name="model_id" ref="model_event_program"/>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

        <!-- Allow base users to see only events from their own company -->
        <record id="rule_event_program_company_filter" model="ir.rule">
            <field name="name">Event/Program: User sees own company events</field>
            <field name="model_id" ref="model_event_program"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
        </record>
    </data>
</odoo>
