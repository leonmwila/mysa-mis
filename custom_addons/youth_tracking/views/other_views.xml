<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Program Form View -->
    <record id="view_youth_program_form" model="ir.ui.view">
        <field name="name">youth.program.form</field>
        <field name="model">youth.program</field>
        <field name="arch" type="xml">
            <form string="Youth Program">
                <header>
                    <button name="action_start_program" type="object" string="Activate" 
                           class="btn-primary" invisible="status == 'active'"/>
                    <button name="action_complete_program" type="object" string="Complete" 
                           class="btn-success" invisible="status != 'active'"/>
                    <field name="status" widget="statusbar" statusbar_visible="planning,active,completed"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_participants" type="object" 
                                class="oe_stat_button" icon="fa-users">
                            <field name="current_participants" widget="statbutton" string="Enrolled"/>
                        </button>
                        <button name="action_generate_certificates" type="object" 
                                class="oe_stat_button" icon="fa-graduation-cap">
                            <field name="completion_rate" widget="statbutton" string="Completed"/>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Program Name" required="1"/>
                        </h1>
                        <div class="o_row">
                            <field name="program_id" readonly="1"/>
                        </div>
                    </div>

                    <group>
                        <group name="basic_info" string="Program Information">
                            <field name="program_type" required="1"/>
                            <field name="zone_id" options="{'no_create': False}"/>
                            <field name="coordinator_id" options="{'no_create': True}"/>
                            <field name="max_participants"/>
                        </group>
                        <group name="schedule" string="Schedule &amp; Budget">
                            <field name="start_date" required="1"/>
                            <field name="end_date" required="1"/>
                            <field name="duration_days" readonly="1"/>
                            <field name="budget" widget="monetary"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" nolabel="1"/>
                        </page>


                        <page string="Enrollments" name="enrollments">
                            <field name="participant_ids">
                                <list editable="bottom">
                                    <field name="youth_id"/>
                                    <field name="registration_date"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>

                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Program Tree View -->
    <record id="view_youth_program_tree" model="ir.ui.view">
        <field name="name">youth.program.tree</field>
        <field name="model">youth.program</field>
        <field name="arch" type="xml">
            <list string="Youth Programs">
                <field name="name"/>
                <field name="program_type"/>
                <field name="zone_id"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="current_participants"/>
                <field name="max_participants"/>
                <field name="budget" widget="monetary"/>
                <field name="status" widget="badge" decoration-info="status == 'planning'" decoration-success="status == 'active'" decoration-muted="status == 'completed'"/>
            </list>
        </field>
    </record>

    <!-- Organization Form View -->
    <record id="view_youth_organization_form" model="ir.ui.view">
        <field name="name">youth.organization.form</field>
        <field name="model">youth.organization</field>
        <field name="arch" type="xml">
            <form string="Youth Organization">
                <header>
                    <button name="action_approve_organization" type="object" string="Activate" 
                           class="btn-primary" invisible="registration_status == 'certified'"/>
                    <button name="action_suspend_organization" type="object" string="Suspend" 
                           class="btn-secondary" invisible="registration_status == 'suspended'"/>
                    <field name="registration_status" widget="statusbar" statusbar_visible="pending,certified,suspended"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_members" type="object" 
                                class="oe_stat_button" icon="fa-users">
                            <field name="total_members" widget="statbutton" string="Members"/>
                        </button>
                        <button name="action_view_programs" type="object" 
                                class="oe_stat_button" icon="fa-graduation-cap">
                            <field name="total_programs" widget="statbutton" string="Programs"/>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Organization Name" required="1"/>
                        </h1>
                        <div class="o_row">
                            <field name="organization_id" readonly="1"/>
                        </div>
                    </div>

                    <group>
                        <group name="basic_info" string="Organization Information">
                            <field name="organization_type" required="1"/>
                            <field name="zone_id" options="{'no_create': False}"/>
                            <field name="chairperson_id" options="{'no_create': True}"/>
                            <field name="registration_date"/>
                        </group>
                        <group name="contact_info" string="Contact Information">
                            <field name="phone"/>
                            <field name="email"/>
                            <field name="physical_address"/>
                            <field name="postal_address"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" nolabel="1"/>
                        </page>

                        <page string="Members" name="members">
                            <field name="member_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="age"/>
                                    <field name="gender"/>
                                    <field name="phone"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>

                        <page string="Programs" name="programs">
                            <field name="program_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="program_type"/>
                                    <field name="start_date"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>

                        <page string="Goals &amp; Objectives" name="goals">
                            <field name="objectives" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Organization Kanban View -->
    <record id="view_youth_organization_kanban" model="ir.ui.view">
        <field name="name">youth.organization.kanban</field>
        <field name="model">youth.organization</field>
        <field name="arch" type="xml">
            <kanban create="1" edit="1" delete="1">
                <field name="id"/>
                <field name="name"/>
                <field name="organization_type"/>
                <field name="zone_id"/>
                <field name="total_members"/>
                <field name="total_programs"/>
                <field name="registration_status"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                                <div class="o_kanban_record_subtitle">
                                    <field name="organization_type"/> • <field name="zone_id"/>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <span class="badge badge-primary">
                                            <i class="fa fa-users"/> <t t-esc="record.total_members.value"/> Members
                                        </span>
                                        <span class="badge badge-info">
                                            <i class="fa fa-graduation-cap"/> <t t-esc="record.total_programs.value"/> Programs
                                        </span>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <span class="badge badge-success" t-if="record.registration_status.raw_value == 'certified'">Certified</span>
                                        <span class="badge badge-warning" t-if="record.registration_status.raw_value == 'pending'">Pending</span>
                                        <span class="badge badge-secondary" t-if="record.registration_status.raw_value == 'suspended'">Suspended</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Organization Tree View -->
    <record id="view_youth_organization_tree" model="ir.ui.view">
        <field name="name">youth.organization.tree</field>
        <field name="model">youth.organization</field>
        <field name="arch" type="xml">
            <list string="Youth Organizations" create="1" edit="1" delete="1">
                <field name="name"/>
                <field name="organization_type"/>
                <field name="zone_id"/>
                <field name="chairperson_id"/>
                <field name="total_members"/>
                <field name="registration_date"/>
                <field name="registration_status" widget="badge" decoration-success="registration_status == 'certified'" decoration-muted="registration_status == 'suspended'"/>
            </list>
        </field>
    </record>

    <!-- Achievement Views -->
    <record id="view_youth_achievement_form" model="ir.ui.view">
        <field name="name">youth.achievement.form</field>
        <field name="model">youth.achievement</field>
        <field name="arch" type="xml">
            <form string="Youth Achievement">
                <header>
                    <button name="action_verify_achievement" type="object" string="Verify" 
                           class="btn-primary" invisible="verified == True"/>
                    <button name="action_issue_certificate" type="object" string="Issue Certificate" 
                           class="btn-success" invisible="certificate_issued == True or verified == False"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Achievement Title" required="1"/>
                        </h1>
                    </div>

                    <group>
                        <group name="basic_info" string="Achievement Information">
                            <field name="youth_id" required="1"/>
                            <field name="achievement_type" required="1"/>
                            <field name="achievement_level" required="1"/>
                            <field name="achievement_date" required="1"/>
                        </group>
                        <group name="verification" string="Verification &amp; Recognition">
                            <field name="award_given_by"/>
                            <field name="verified" readonly="1"/>
                            <field name="verified_by_id" readonly="1"/>
                            <field name="certificate_issued" readonly="1"/>
                            <field name="certificate_number" readonly="1"/>
                            <field name="points_awarded"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" nolabel="1"/>
                        </page>

                        <page string="Related Programs" name="programs">
                            <group>
                                <field name="program_id"/>
                                <field name="event_id"/>
                            </group>
                        </page>

                        <page string="Notes" name="notes">
                            <field name="notes" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Achievement Tree View -->
    <record id="view_youth_achievement_tree" model="ir.ui.view">
        <field name="name">youth.achievement.tree</field>
        <field name="model">youth.achievement</field>
        <field name="arch" type="xml">
            <list string="Youth Achievements" decoration-success="verified == True">
                <field name="name"/>
                <field name="youth_id"/>
                <field name="achievement_type"/>
                <field name="achievement_level"/>
                <field name="achievement_date"/>
                <field name="points_awarded"/>
                <field name="verified" widget="boolean_button"/>
                <field name="certificate_issued"/>
            </list>
        </field>
    </record>


</odoo>